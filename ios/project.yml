name: OhMyPT
options:
  bundleIdPrefix: com.ohmypt
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.0"
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
targets:
  OhMyPT:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: OhMyPT
    info:
      path: OhMyPT/Info.plist
      properties:
        CFBundleDisplayName: OhMyPT
        UILaunchScreen:
          UIColorName: AccentColor
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        NSCameraUsageDescription: 운동·식단·인바디 사진을 촬영해 기록합니다.
        NSPhotoLibraryUsageDescription: 앨범에서 운동·식단·인바디 사진을 선택해 기록합니다.
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ohmypt.app
        PRODUCT_NAME: OhMyPT
        TARGETED_DEVICE_FAMILY: "1,2"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
